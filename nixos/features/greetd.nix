{
  services.greetd = {
    enable = true;
    settings = {
      default_session = {
        command = "sway";
      };
      initial_session = {
        user = "uwu";
        command = "sway";
      };
    };
  };
}
